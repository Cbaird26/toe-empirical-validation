# Git LFS configuration for large files

# PDF documents
*.pdf filter=lfs diff=lfs merge=lfs -text

# Large image files
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text

# Large data files
*.csv filter=lfs diff=lfs merge=lfs -text
*.json filter=lfs diff=lfs merge=lfs -text

# Large source documents
docs/papers/*.pdf filter=lfs diff=lfs merge=lfs -text
docs/papers/*.docx filter=lfs diff=lfs merge=lfs -text

# Results directory (large plots and data)
results/**/*.png filter=lfs diff=lfs merge=lfs -text
results/**/*.pdf filter=lfs diff=lfs merge=lfs -text
results/**/*.csv filter=lfs diff=lfs merge=lfs -text

# Keep LaTeX source files as text
*.tex text
*.bib text
*.cls text
*.sty text

# Keep code files as text
*.py text
*.sh text
*.yaml text
*.yml text
*.md text
*.json text eol=lf
